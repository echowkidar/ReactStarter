# Database settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=salary_db
DB_USER=postgres
DB_PASSWORD=postgres

# Real SMTP Settings
# Uncomment and fill these to use a real SMTP server instead of Ethereal test emails
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM="AMU Salary Section <your-email@gmail.com>"

# For accepting self-signed certificates (development only)
# SMTP_REJECT_UNAUTHORIZED=false 